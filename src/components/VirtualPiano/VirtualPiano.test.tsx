import React from 'react';
import { VirtualPiano } from './VirtualPiano.component';

describe('VirtualPiano component', () => {
  it('defines VirtualPiano component successfully', () => {
    expect(VirtualPiano).toBeDefined();
  });
});
