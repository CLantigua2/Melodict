import React from 'react';
import { ExportModal } from './ExportModal.component';

describe('ExportModal component', () => {
  it('defines ExportModal component successfully', () => {
    expect(ExportModal).toBeDefined();
  });
});
