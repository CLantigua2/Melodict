import React from 'react';
import { NotationCanvas } from './NotationCanvas.component';

describe('NotationCanvas component', () => {
  it('defines NotationCanvas component successfully', () => {
    expect(NotationCanvas).toBeDefined();
  });
});
