import React from 'react';
import { EditableText } from './EditableText.component';

describe('EditableText component', () => {
  it('defines EditableText component successfully', () => {
    expect(EditableText).toBeDefined();
  });
});
